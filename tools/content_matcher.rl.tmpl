// AUTOGENERATED BY ITTYFS.PY ON ###GENDATE###

#include <stdio.h>
#include <string.h>

#include "content.h"

###DATABLOCKS###

###CONTENTBLOCKS###


%%{
	machine content_match;
	access ;
	
	main := (
###MATCHERS###
		);
}%%

%% write data;

const itty_content_t* itty_content_get(http_dispatch_verb_t verb, const char* path, int path_length) {
	int cs;
	const itty_content_t* content = NULL;
	const char *p = path;
	const char *pe = path + path_length;
	const char *eof = pe;

	%% write init;

	%% write exec;

	return (cs == content_match_error) ? NULL : content;
}

